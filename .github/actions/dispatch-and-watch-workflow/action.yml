name: Dispatch and watch workflow
description: Dispatches a workflow_dispatch in another repository and waits for it to complete.

inputs:
  repo:
    description: Target repository in `owner/name` form.
    required: true
  workflow:
    description: Workflow filename (e.g. `release-cider.yaml`) or ID.
    required: true
  ref:
    description: Git ref to dispatch the workflow against.
    required: false
    default: main
  version:
    description: Version passed as the `version` input to the dispatched workflow.
    required: true
  is_stable:
    description: Whether the release is a stable (X.Y.Z) version. Forwarded as the `is_stable` input.
    required: true
  token:
    description: "Token with `actions: write` on the target repository."
    required: true

outputs:
  run-id:
    description: Numeric ID of the dispatched run.
    value: ${{ steps.dispatch.outputs.run-id }}
  run-url:
    description: HTML URL of the dispatched run.
    value: ${{ steps.dispatch.outputs.run-url }}

runs:
  using: composite
  steps:
    - name: Dispatch workflow
      id: dispatch
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.token }}
        REPO: ${{ inputs.repo }}
        WORKFLOW: ${{ inputs.workflow }}
        REF: ${{ inputs.ref }}
        VERSION: ${{ inputs.version }}
        IS_STABLE: ${{ inputs.is_stable }}
      run: |
        set -euo pipefail

        RUN_URL=$(gh workflow run "$WORKFLOW" --repo "$REPO" --ref "$REF" \
          -f version="$VERSION" \
          -f is_stable="$IS_STABLE")

        if [ -z "$RUN_URL" ]; then
          echo "::error::Dispatch did not return a run URL. Requires gh >= 2.87.0 against github.com."
          exit 1
        fi

        RUN_ID="${RUN_URL##*/}"

        echo "run-id=$RUN_ID" >> "$GITHUB_OUTPUT"
        echo "run-url=$RUN_URL" >> "$GITHUB_OUTPUT"
        echo "Dispatched run: $RUN_URL"

    - name: Watch run
      shell: bash
      env:
        GH_TOKEN: ${{ inputs.token }}
        REPO: ${{ inputs.repo }}
        RUN_ID: ${{ steps.dispatch.outputs.run-id }}
      run: |
        set -euo pipefail

        for attempt in 1 2 3; do
          gh run watch "$RUN_ID" --repo "$REPO" --exit-status --compact && exit 0
          echo "Watch attempt $attempt/3 failed."
          sleep 10
        done

        echo "::error::Run $RUN_ID failed, or its status could not be read."
        exit 1
